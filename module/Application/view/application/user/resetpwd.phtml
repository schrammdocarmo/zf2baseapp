<h1><?php echo $this->translate('Reset password'); ?></h1>
<hr/>
<br/>

<div class="container">

<?php
if ($this->form) {
$form = $this->form;
$form->setAttribute('action', $this->url('resetpwd'))->prepare();
echo $this->form()->openTag($form);
echo $this->form()->render($form);
echo $this->form()->closeTag($form);
}
if ($this->email) {
echo sprintf($this->translate('The password for %s has been updated'),$this->email);
}
?>
</div>
